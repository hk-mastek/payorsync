export const PAYORS = [
  { id: 'p1', name: 'Medicare Traditional - Noridian', type: 'Medicare Traditional', region: 'West' },
  { id: 'p2', name: 'Medicare Traditional - CGS', type: 'Medicare Traditional', region: 'Midwest' },
  { id: 'p3', name: 'Medicare Traditional - Palmetto', type: 'Medicare Traditional', region: 'South' },
  { id: 'p4', name: 'Medicare Traditional - NGS', type: 'Medicare Traditional', region: 'Northeast' },
  { id: 'p5', name: 'Medicare Traditional - WPS', type: 'Medicare Traditional', region: 'Central' },
  { id: 'p6', name: 'UnitedHealthcare Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p7', name: 'Humana Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p8', name: 'Aetna Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p9', name: 'Cigna Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p10', name: 'Anthem Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p11', name: 'Kaiser Medicare Advantage', type: 'Medicare Advantage', region: 'West' },
  { id: 'p12', name: 'WellCare Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p13', name: 'Molina Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p14', name: 'Centene Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p15', name: 'CVS Health Medicare Advantage', type: 'Medicare Advantage', region: 'National' },
  { id: 'p16', name: 'California Medicaid (Medi-Cal)', type: 'Medicaid', region: 'CA' },
  { id: 'p17', name: 'Texas Medicaid', type: 'Medicaid', region: 'TX' },
  { id: 'p18', name: 'Florida Medicaid', type: 'Medicaid', region: 'FL' },
  { id: 'p19', name: 'New York Medicaid', type: 'Medicaid', region: 'NY' },
  { id: 'p20', name: 'Pennsylvania Medicaid', type: 'Medicaid', region: 'PA' },
  { id: 'p21', name: 'Illinois Medicaid', type: 'Medicaid', region: 'IL' },
  { id: 'p22', name: 'Ohio Medicaid', type: 'Medicaid', region: 'OH' },
  { id: 'p23', name: 'Georgia Medicaid', type: 'Medicaid', region: 'GA' },
  { id: 'p24', name: 'North Carolina Medicaid', type: 'Medicaid', region: 'NC' },
  { id: 'p25', name: 'Michigan Medicaid', type: 'Medicaid', region: 'MI' },
  { id: 'p26', name: 'UnitedHealthcare', type: 'Commercial PPO', region: 'National' },
  { id: 'p27', name: 'Cigna HealthCare', type: 'Commercial PPO', region: 'National' },
  { id: 'p28', name: 'Aetna', type: 'Commercial PPO', region: 'National' },
  { id: 'p29', name: 'Humana Commercial', type: 'Commercial HMO', region: 'National' },
  { id: 'p30', name: 'Anthem Blue Cross', type: 'Commercial PPO', region: 'National' },
  { id: 'p31', name: 'Kaiser Permanente', type: 'Commercial HMO', region: 'West' },
  { id: 'p32', name: 'Centene Commercial', type: 'Commercial EPO', region: 'National' },
  { id: 'p33', name: 'Molina Healthcare', type: 'Commercial HMO', region: 'National' },
  { id: 'p34', name: 'WellCare Commercial', type: 'Commercial PPO', region: 'National' },
  { id: 'p35', name: 'Health Net', type: 'Commercial HMO', region: 'West' },
  { id: 'p36', name: 'Oscar Health', type: 'Commercial EPO', region: 'National' },
  { id: 'p37', name: 'Bright Health', type: 'Commercial EPO', region: 'National' },
  { id: 'p38', name: 'Clover Health', type: 'Commercial HMO', region: 'National' },
  { id: 'p39', name: 'Devoted Health', type: 'Commercial PPO', region: 'National' },
  { id: 'p40', name: 'Alignment Healthcare', type: 'Commercial HMO', region: 'West' },
  { id: 'p41', name: 'BCBS of Texas', type: 'Commercial PPO', region: 'TX' },
  { id: 'p42', name: 'BCBS of Florida', type: 'Commercial PPO', region: 'FL' },
  { id: 'p43', name: 'BCBS of California', type: 'Commercial PPO', region: 'CA' },
  { id: 'p44', name: 'BCBS of New York', type: 'Commercial PPO', region: 'NY' },
  { id: 'p45', name: 'BCBS of Pennsylvania', type: 'Commercial PPO', region: 'PA' },
  { id: 'p46', name: 'BCBS of Illinois', type: 'Commercial PPO', region: 'IL' },
  { id: 'p47', name: 'BCBS of Ohio', type: 'Commercial PPO', region: 'OH' },
  { id: 'p48', name: 'BCBS of Georgia', type: 'Commercial PPO', region: 'GA' },
  { id: 'p49', name: 'BCBS of North Carolina', type: 'Commercial PPO', region: 'NC' },
  { id: 'p50', name: 'BCBS of Michigan', type: 'Commercial PPO', region: 'MI' },
  { id: 'p51', name: 'BCBS of Tennessee', type: 'Commercial PPO', region: 'TN' },
  { id: 'p52', name: 'BCBS of Arizona', type: 'Commercial PPO', region: 'AZ' },
  { id: 'p53', name: 'BCBS of Massachusetts', type: 'Commercial PPO', region: 'MA' },
  { id: 'p54', name: 'BCBS of Virginia', type: 'Commercial PPO', region: 'VA' },
  { id: 'p55', name: 'BCBS of Washington', type: 'Commercial PPO', region: 'WA' },
  { id: 'p56', name: 'BCBS of New Jersey', type: 'Commercial PPO', region: 'NJ' },
  { id: 'p57', name: 'BCBS of Indiana', type: 'Commercial PPO', region: 'IN' },
  { id: 'p58', name: 'BCBS of Missouri', type: 'Commercial PPO', region: 'MO' },
  { id: 'p59', name: 'BCBS of Maryland', type: 'Commercial PPO', region: 'MD' },
  { id: 'p60', name: 'BCBS of Wisconsin', type: 'Commercial PPO', region: 'WI' },
  { id: 'p61', name: 'Priority Health', type: 'Commercial HMO', region: 'MI' },
  { id: 'p62', name: 'SelectHealth', type: 'Commercial PPO', region: 'UT' },
  { id: 'p63', name: 'Presbyterian Health Plan', type: 'Commercial HMO', region: 'NM' },
  { id: 'p64', name: 'Tufts Health Plan', type: 'Commercial PPO', region: 'MA' },
  { id: 'p65', name: 'Harvard Pilgrim', type: 'Commercial PPO', region: 'MA' },
  { id: 'p66', name: 'Medical Mutual', type: 'Commercial PPO', region: 'OH' },
  { id: 'p67', name: 'Highmark', type: 'Commercial PPO', region: 'PA' },
  { id: 'p68', name: 'Independence Blue Cross', type: 'Commercial PPO', region: 'PA' },
  { id: 'p69', name: 'Horizon BCBS', type: 'Commercial PPO', region: 'NJ' },
  { id: 'p70', name: 'Florida Blue', type: 'Commercial PPO', region: 'FL' },
  { id: 'p71', name: 'VA Healthcare', type: 'VA', region: 'National' },
  { id: 'p72', name: 'TRICARE East', type: 'TRICARE', region: 'East' },
  { id: 'p73', name: 'TRICARE West', type: 'TRICARE', region: 'West' },
  { id: 'p74', name: 'Managed Medicaid - Molina CA', type: 'Medicaid Managed Care', region: 'CA' },
  { id: 'p75', name: 'Managed Medicaid - UHC TX', type: 'Medicaid Managed Care', region: 'TX' },
];

export const STATES = [
  { code: 'CA', name: 'California', weight: 12 },
  { code: 'TX', name: 'Texas', weight: 11 },
  { code: 'FL', name: 'Florida', weight: 10 },
  { code: 'NY', name: 'New York', weight: 8 },
  { code: 'PA', name: 'Pennsylvania', weight: 7 },
  { code: 'IL', name: 'Illinois', weight: 6 },
  { code: 'OH', name: 'Ohio', weight: 5 },
  { code: 'GA', name: 'Georgia', weight: 4 },
  { code: 'NC', name: 'North Carolina', weight: 4 },
  { code: 'MI', name: 'Michigan', weight: 3 },
  { code: 'NJ', name: 'New Jersey', weight: 3 },
  { code: 'VA', name: 'Virginia', weight: 2.5 },
  { code: 'WA', name: 'Washington', weight: 2.5 },
  { code: 'AZ', name: 'Arizona', weight: 2.5 },
  { code: 'MA', name: 'Massachusetts', weight: 2.5 },
  { code: 'TN', name: 'Tennessee', weight: 2 },
  { code: 'IN', name: 'Indiana', weight: 2 },
  { code: 'MO', name: 'Missouri', weight: 2 },
  { code: 'MD', name: 'Maryland', weight: 2 },
  { code: 'WI', name: 'Wisconsin', weight: 2 },
  { code: 'CO', name: 'Colorado', weight: 1.5 },
  { code: 'MN', name: 'Minnesota', weight: 1.5 },
  { code: 'SC', name: 'South Carolina', weight: 1.5 },
  { code: 'AL', name: 'Alabama', weight: 1.5 },
  { code: 'LA', name: 'Louisiana', weight: 1.5 },
];

export const ROOT_CAUSES = [
  { category: 'Authorization Issues', subcategories: ['Missing auth', 'Expired auth', 'Auth limit exceeded'], weight: 28 },
  { category: 'Contractual Disputes', subcategories: ['Rate discrepancy', 'Fee schedule mismatch', 'Contract not loaded'], weight: 22 },
  { category: 'Coding Errors', subcategories: ['Incorrect CPT', 'Incorrect ICD-10', 'Modifier issues'], weight: 18 },
  { category: 'Patient Eligibility', subcategories: ['Coverage terminated', 'COB issues', 'Wrong plan'], weight: 15 },
  { category: 'Medical Necessity', subcategories: ['LCD/NCD denial', 'Documentation insufficient'], weight: 10 },
  { category: 'Claim Submission Errors', subcategories: ['Duplicate claim', 'Missing information', 'Late filing'], weight: 4 },
  { category: 'Timely Filing', subcategories: ['Past deadline', 'Appeal deadline missed'], weight: 2 },
  { category: 'System/Technical', subcategories: ['EDI rejection', 'Payer system error', 'Clearinghouse issue'], weight: 1 },
];

export const AGING_BUCKETS = [
  { label: '0-30 days', min: 0, max: 30, weight: 25 },
  { label: '31-60 days', min: 31, max: 60, weight: 22 },
  { label: '61-90 days', min: 61, max: 90, weight: 18 },
  { label: '91-120 days', min: 91, max: 120, weight: 15 },
  { label: '121-180 days', min: 121, max: 180, weight: 12 },
  { label: '181-365 days', min: 181, max: 365, weight: 6 },
  { label: '365+ days', min: 366, max: 730, weight: 2 },
];

export const STATUSES = [
  { value: 'Open', weight: 45 },
  { value: 'In Progress', weight: 30 },
  { value: 'Under Appeal', weight: 15 },
  { value: 'Resolved', weight: 8 },
  { value: 'Written Off', weight: 2 },
];

export const ANALYSTS = [
  'Sarah Johnson', 'Michael Chen', 'Emily Rodriguez', 'James Williams', 'Jessica Martinez',
  'David Thompson', 'Amanda Garcia', 'Christopher Lee', 'Ashley Brown', 'Matthew Wilson',
  'Lauren Davis', 'Ryan Miller', 'Stephanie Taylor', 'Kevin Anderson', 'Michelle Thomas'
];

export const PAYOR_TYPES = [
  { type: 'Medicare Traditional', weight: 35 },
  { type: 'Medicare Advantage', weight: 25 },
  { type: 'Medicaid', weight: 12 },
  { type: 'Medicaid Managed Care', weight: 8 },
  { type: 'Commercial PPO', weight: 10 },
  { type: 'Commercial HMO', weight: 5 },
  { type: 'Commercial EPO', weight: 2 },
  { type: 'VA', weight: 2 },
  { type: 'TRICARE', weight: 1 },
];

function seededRandom(seed: number) {
  const x = Math.sin(seed) * 10000;
  return x - Math.floor(x);
}

function weightedRandomSelect<T extends { weight: number }>(items: T[], seed: number): T {
  const totalWeight = items.reduce((sum, item) => sum + item.weight, 0);
  let random = seededRandom(seed) * totalWeight;
  for (const item of items) {
    random -= item.weight;
    if (random <= 0) return item;
  }
  return items[items.length - 1];
}

export interface VarianceRecord {
  id: string;
  payorId: string;
  payorName: string;
  payorType: string;
  state: string;
  stateName: string;
  rootCauseCategory: string;
  rootCauseSubcategory: string;
  billedAmount: number;
  paidAmount: number;
  varianceAmount: number;
  variancePercentage: number;
  dateOfService: string;
  claimSubmissionDate: string;
  varianceIdentifiedDate: string;
  status: string;
  agingBucket: string;
  agingDays: number;
  assignedAnalyst: string;
  priority: 'High' | 'Medium' | 'Low';
  treatmentCount: number;
}

export function generateVarianceData(count: number = 2250): VarianceRecord[] {
  const records: VarianceRecord[] = [];
  const baseDate = new Date('2025-01-15');
  
  for (let i = 0; i < count; i++) {
    const seed = i + 1;
    
    const payorType = weightedRandomSelect(PAYOR_TYPES, seed);
    const matchingPayors = PAYORS.filter(p => p.type === payorType.type);
    const payor = matchingPayors[Math.floor(seededRandom(seed * 2) * matchingPayors.length)] || PAYORS[0];
    
    const state = weightedRandomSelect(STATES.map(s => ({ ...s, weight: s.weight })), seed * 3);
    const rootCause = weightedRandomSelect(ROOT_CAUSES, seed * 4);
    const subcategory = rootCause.subcategories[Math.floor(seededRandom(seed * 5) * rootCause.subcategories.length)];
    const agingBucket = weightedRandomSelect(AGING_BUCKETS, seed * 6);
    const status = weightedRandomSelect(STATUSES, seed * 7);
    
    const billedAmount = Math.round((150 + seededRandom(seed * 8) * 2350) * 100) / 100;
    const variancePercentage = 5 + seededRandom(seed * 9) * 45;
    const varianceAmount = Math.round(billedAmount * (variancePercentage / 100) * 100) / 100;
    const paidAmount = Math.round((billedAmount - varianceAmount) * 100) / 100;
    
    const agingDays = agingBucket.min + Math.floor(seededRandom(seed * 10) * (agingBucket.max - agingBucket.min));
    const varianceDate = new Date(baseDate);
    varianceDate.setDate(varianceDate.getDate() - agingDays);
    
    const dosDate = new Date(varianceDate);
    dosDate.setDate(dosDate.getDate() - Math.floor(seededRandom(seed * 11) * 30) - 7);
    
    const claimDate = new Date(dosDate);
    claimDate.setDate(claimDate.getDate() + Math.floor(seededRandom(seed * 12) * 14) + 1);
    
    let priority: 'High' | 'Medium' | 'Low' = 'Low';
    if (varianceAmount > 1000 || agingDays > 120) priority = 'High';
    else if (varianceAmount > 500 || agingDays > 60) priority = 'Medium';
    
    records.push({
      id: `V-${String(10001 + i).padStart(5, '0')}`,
      payorId: payor.id,
      payorName: payor.name,
      payorType: payor.type,
      state: state.code,
      stateName: state.name,
      rootCauseCategory: rootCause.category,
      rootCauseSubcategory: subcategory,
      billedAmount,
      paidAmount,
      varianceAmount,
      variancePercentage: Math.round(variancePercentage * 10) / 10,
      dateOfService: dosDate.toISOString().split('T')[0],
      claimSubmissionDate: claimDate.toISOString().split('T')[0],
      varianceIdentifiedDate: varianceDate.toISOString().split('T')[0],
      status: status.value,
      agingBucket: agingBucket.label,
      agingDays,
      assignedAnalyst: ANALYSTS[Math.floor(seededRandom(seed * 13) * ANALYSTS.length)],
      priority,
      treatmentCount: 1 + Math.floor(seededRandom(seed * 14) * 5),
    });
  }
  
  return records;
}

export function calculateKPIs(data: VarianceRecord[]) {
  const openVariances = data.filter(v => v.status !== 'Resolved' && v.status !== 'Written Off');
  const resolvedVariances = data.filter(v => v.status === 'Resolved');
  const writtenOff = data.filter(v => v.status === 'Written Off');
  
  const today = new Date('2025-01-15');
  const weekAgo = new Date(today);
  weekAgo.setDate(weekAgo.getDate() - 7);
  
  const newThisWeek = data.filter(v => new Date(v.varianceIdentifiedDate) >= weekAgo).length;
  const resolvedThisWeek = resolvedVariances.filter(v => {
    const date = new Date(v.varianceIdentifiedDate);
    date.setDate(date.getDate() + v.agingDays);
    return date >= weekAgo;
  }).length;
  
  const totalVarianceAmount = openVariances.reduce((sum, v) => sum + v.varianceAmount, 0);
  const avgDaysToResolution = resolvedVariances.length > 0 
    ? resolvedVariances.reduce((sum, v) => sum + v.agingDays, 0) / resolvedVariances.length 
    : 0;
  
  const totalRecoverable = [...resolvedVariances, ...writtenOff].reduce((sum, v) => sum + v.varianceAmount, 0);
  const recovered = resolvedVariances.reduce((sum, v) => sum + v.varianceAmount, 0);
  const recoveryRate = totalRecoverable > 0 ? (recovered / totalRecoverable) * 100 : 0;
  
  return {
    totalOpenVariances: openVariances.length,
    totalVarianceAmount,
    newThisWeek,
    resolvedThisWeek,
    avgDaysToResolution: Math.round(avgDaysToResolution * 10) / 10,
    recoveryRate: Math.round(recoveryRate * 10) / 10,
  };
}

export function groupByPayorType(data: VarianceRecord[]) {
  const groups: Record<string, { count: number; amount: number }> = {};
  data.forEach(v => {
    if (!groups[v.payorType]) groups[v.payorType] = { count: 0, amount: 0 };
    groups[v.payorType].count++;
    groups[v.payorType].amount += v.varianceAmount;
  });
  return Object.entries(groups)
    .map(([type, data]) => ({ type, ...data }))
    .sort((a, b) => b.amount - a.amount);
}

export function groupByState(data: VarianceRecord[]) {
  const groups: Record<string, { count: number; amount: number; stateName: string }> = {};
  data.forEach(v => {
    if (!groups[v.state]) groups[v.state] = { count: 0, amount: 0, stateName: v.stateName };
    groups[v.state].count++;
    groups[v.state].amount += v.varianceAmount;
  });
  return Object.entries(groups)
    .map(([state, data]) => ({ state, ...data }))
    .sort((a, b) => b.amount - a.amount);
}

export function groupByRootCause(data: VarianceRecord[]) {
  const groups: Record<string, { count: number; amount: number; subcategories: Record<string, { count: number; amount: number }> }> = {};
  data.forEach(v => {
    if (!groups[v.rootCauseCategory]) {
      groups[v.rootCauseCategory] = { count: 0, amount: 0, subcategories: {} };
    }
    groups[v.rootCauseCategory].count++;
    groups[v.rootCauseCategory].amount += v.varianceAmount;
    
    if (!groups[v.rootCauseCategory].subcategories[v.rootCauseSubcategory]) {
      groups[v.rootCauseCategory].subcategories[v.rootCauseSubcategory] = { count: 0, amount: 0 };
    }
    groups[v.rootCauseCategory].subcategories[v.rootCauseSubcategory].count++;
    groups[v.rootCauseCategory].subcategories[v.rootCauseSubcategory].amount += v.varianceAmount;
  });
  return Object.entries(groups)
    .map(([category, data]) => ({
      category,
      count: data.count,
      amount: data.amount,
      subcategories: Object.entries(data.subcategories).map(([name, sub]) => ({ name, ...sub })),
    }))
    .sort((a, b) => b.amount - a.amount);
}

export function groupByAgingBucket(data: VarianceRecord[]) {
  const bucketOrder = ['0-30 days', '31-60 days', '61-90 days', '91-120 days', '121-180 days', '181-365 days', '365+ days'];
  const groups: Record<string, { count: number; amount: number }> = {};
  bucketOrder.forEach(b => groups[b] = { count: 0, amount: 0 });
  
  data.forEach(v => {
    if (groups[v.agingBucket]) {
      groups[v.agingBucket].count++;
      groups[v.agingBucket].amount += v.varianceAmount;
    }
  });
  
  return bucketOrder.map(bucket => ({ bucket, ...groups[bucket] }));
}

export function getTrendData(data: VarianceRecord[]) {
  const months: Record<string, { new: number; resolved: number; amount: number }> = {};
  const baseDate = new Date('2025-01-15');
  
  for (let i = 11; i >= 0; i--) {
    const date = new Date(baseDate);
    date.setMonth(date.getMonth() - i);
    const key = date.toISOString().slice(0, 7);
    months[key] = { new: 0, resolved: 0, amount: 0 };
  }
  
  data.forEach(v => {
    const month = v.varianceIdentifiedDate.slice(0, 7);
    if (months[month]) {
      months[month].new++;
      if (v.status === 'Resolved') months[month].resolved++;
      months[month].amount += v.varianceAmount;
    }
  });
  
  return Object.entries(months).map(([month, data]) => ({
    month: new Date(month + '-01').toLocaleDateString('en-US', { month: 'short', year: '2-digit' }),
    ...data,
    net: data.new - data.resolved,
  }));
}
